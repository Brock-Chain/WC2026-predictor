# Data (downloadable / regenerable — not version controlled)
data/raw/
data/processed/

# Fitted model artifacts (large NetCDF traces)
models/*.nc
models/*.netcdf

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Backtest run logs (the results .json/.txt are kept; raw run logs are noise)
reports/*.log

# OS / editor
.DS_Store
Thumbs.db
.vscode/
